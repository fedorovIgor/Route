import { DayRoute } from "../../models/DayRoutes";
import { Visit } from "../../models/Visit";

export interface VisitComponentInterface {

    // удаляет визит из дневного маршрута
    // вызывает эвент удаления визита
    //deleteVisitFromDay(day: DayRoute, visit: Visit): void;

    
}